extends ./layout/index

block contenido
     h1(class="mt-5 text-center")= pagina
     //- h1 #{pagina} otra forma de llamar una variable en pug
     main.container.mt-5
          include ./layout/bloque_viajes